/* import { RegistryReference } from "datapm-lib";
import { PackageFileWithContext } from "../../util/PackageAccessUtil";
import { CredentialsByPackageIdentifier, PublishProgress } from "../PublishPackageCommandModule";

export function publishData(
    packageFileWithContext: PackageFileWithContext,
    targetRegistries: RegistryReference[],
    credentialsBySourceSlug: CredentialsByPackageIdentifier,
    context: PublishProgress
): Promise<Map<RegistryReference, boolean>> {
    // loop over targetRegistries

    for (const registry of targetRegistries) {
        

        // get a stream of the source
        for(const source of packageFileWithContext.package.sources){
        


        }

    }
}
 */
