overwrite: true
schema: "node_modules/datapm-lib/schema.gql"
documents: src/graphql/*.graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-apollo-angular"
