query UsersByCatalog($identifier: CatalogIdentifierInput!) {
    usersByCatalog(identifier: $identifier) {
        user {
            username
            firstName
            lastName
            nameIsPublic
            displayName
        }
        permissions
        packagePermissions
    }
}
