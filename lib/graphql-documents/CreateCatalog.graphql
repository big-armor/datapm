mutation CreateCatalog($value: CreateCatalogInput!) {
    createCatalog(value: $value) {
        identifier {
            catalogSlug
        }
        myPermissions
        displayName
        description
        website
        isPublic
        unclaimed
    }
}
