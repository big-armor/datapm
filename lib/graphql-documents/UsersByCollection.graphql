query UsersByCollection($identifier: CollectionIdentifierInput!) {
    usersByCollection(identifier: $identifier) {
        user {
            username
            firstName
            lastName
            nameIsPublic
            displayName
        }
        permissions
    }
}
