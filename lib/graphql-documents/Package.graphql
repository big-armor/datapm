query Package($identifier: PackageIdentifierInput!) {
    package(identifier: $identifier) {
        identifier {
            catalogSlug
            packageSlug
        }
        catalog {
            displayName
        }
        displayName
        description
        latestVersion {
            identifier {
                versionMajor
                versionMinor
                versionPatch
            }
            packageFile
            readmeFile
            licenseFile
        }
    }
}
