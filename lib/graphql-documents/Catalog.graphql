query GetCatalog($identifier: CatalogIdentifierInput!) {
    catalog(identifier: $identifier) {
        identifier {
            registryURL
            catalogSlug
        }
        displayName
        description
        website
        myPermissions
        isPublic
        unclaimed
    }
}
